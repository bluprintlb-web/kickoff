# Leader Sport — Context Handoff

## Project Overview
Custom-built football store website with two sides: a customer storefront and an admin dashboard (product/order/stock management, plus in-store POS). Products: kits/jerseys, trophies, balls, gloves, body/leggings, shin pads, grip socks (long & short), and normal socks. Store is based in **Lebanon**.

## Decisions Made
- Built custom (not a no-code platform like Shopify/WooCommerce) for full control.
- Architecture: single Next.js app — public storefront routes + a role-gated `/admin` section — rather than a monorepo with separate apps. Revisit only if the team/scale grows significantly.
- Stack:
  - Frontend: Next.js 16 + TypeScript (App Router)
  - Styling: Tailwind CSS v4 + shadcn/ui (built on **Base UI** — `@base-ui/react` — not Radix, in this shadcn version)
  - API: tRPC v11 (end-to-end type safety) + TanStack Query v5
  - Database: PostgreSQL
  - ORM: Prisma 7
  - Auth: Auth.js v5 (`next-auth@beta`) — Credentials provider, JWT sessions, Prisma adapter, `role` field (`CUSTOMER` / `ADMIN`)
  - Payments: **Whish Pay** (Whish wallet + Visa/Mastercard via their hosted checkout) — see "Payments" below, this is not fully wired up yet
  - Barcode scanning: `@zxing/browser` (camera-based, works cross-browser — no reliance on the experimental native `BarcodeDetector` API)
  - Image storage: Cloudinary (not yet wired up)
  - Hosting: Vercel (app) + Neon or Supabase (Postgres)
- Data model: `Product` → `ProductVariant` (SKU, size, color, barcode, stock, price override) to handle jersey sizes, sock length, glove sizes, etc. — not flat products.
- Sizing (`src/lib/sizes.ts`): three fixed size ladders, but each is now tied to *category* in the admin form (2026-07-13 change, at user's request — "sizes in jerseys only"), not just `ageGroup`:
  - `KIDS`: 20, 22, 24, 26, 28, 30 — only offered when Category = Jersey and "Mens or kids?" = Kids (`Product.ageGroup = KIDS`)
  - `ADULT` (mens): S, M, L, XL, XXL (2XL) — only offered when Category = Jersey and "Mens or kids?" = Mens (`Product.ageGroup = ADULT`)
  - `GLOVES`: 4, 5, 6, 7, 8, 9, 10, 11 — always offered when Category = Gloves, no age-group split (`Product.ageGroup` stays unset for gloves; the ladder is chosen by category alone in the form, not stored as a DB enum value)
  - Every other category (Trophy, Ball, Bodywear, Shin pads, Socks) has no fixed ladder — `ProductVariant.size` stays free text via a manual add/remove variant list, same as before.
- Mutations (signup/login/logout) use native `<form>` + Server Actions + `useActionState`, not react-hook-form — this is Next.js 16's own recommended pattern and avoids an extra dependency. Cart/checkout/POS/admin-product mutations use tRPC client-side mutations instead, since they're more interactive (dynamic rows, live scanning) than a one-shot form submit.
- Authorization is two-layered per Next.js's own security guidance: `src/proxy.ts` does a cheap, optimistic JWT-cookie check to redirect obviously unauthenticated/non-admin requests away from `/admin`; the real check happens in `src/lib/dal.ts` (`requireAdmin()`) and in `adminProcedure`/`protectedProcedure` (`src/server/trpc.ts`), called from every Server Action/Route Handler/tRPC procedure that touches sensitive data. Proxy alone is never trusted for security.
- Orders have a `channel` (`ONLINE` vs `IN_STORE`) and a `paymentMethod` (`WHISH` / `CARD` / `CASH`). `Order.userId` and the shipping fields are optional because POS/walk-in sales have neither.
- **Only one User can hold `role = ADMIN` at a time.** Enforced with a Postgres partial unique index (`CREATE UNIQUE INDEX ... ON "User" ("role") WHERE "role" = 'ADMIN'`), hand-written in `prisma/migrations/20260709230430_single_admin_constraint/migration.sql` since Prisma's schema DSL can't express a filtered unique constraint. Holds even against direct SQL/Prisma Studio edits, not just app code. Verified by actually trying to create a second admin and confirming Postgres rejects it. There's still no in-app way to *become* admin — it's a manual DB edit either way, this just makes it impossible to have two.
- UI theme (redesigned 2026-07-10, at user's request): charcoal `#343a40` primary (header bar, buttons, headings) + gold accent (CTAs, cart badge, logo highlight) + pitch-green `--brand` token kept for prices/success (`src/app/globals.css`, oklch-based, light + dark). Neutral scale (secondary/muted/border/ring) shifted from green-tinted to charcoal-tinted hue (~248) to stay cohesive with the new primary. Added `accent` variants to `Button` and `Badge` (`src/components/ui/button.tsx`, `badge.tsx`) for gold CTAs/badges. Site header (`src/components/site-header.tsx`) is now a solid charcoal bar with a two-tone "Leader **Sport**" wordmark instead of a translucent white bar with gradient text. Product cards were consolidated into one shared `src/components/product-card.tsx` (previously duplicated between `/` and `/products`) with a category → lucide-icon mapping (Shirt/Trophy/Volleyball/Hand/PersonStanding/Shield/Footprints) shown in a small tinted icon tile. Entrance/hover animations via `tw-animate-css` unchanged.
- Storefront UI, second pass (2026-07-10, "search online for similar sites and use the best UI"): screenshotted real football-retail homepages (Pro:Direct Soccer rendered cleanly; Classic Football Shirts and Unisport both blocked headless Chrome — Cloudflare bot-detection and an indefinite hang respectively, so those were abandoned rather than fought) plus general sports-ecommerce write-ups (Aether Apparel, Route One, Optimistic Runners) for structural patterns, then adapted what was feasible without real product photography (Cloudinary still isn't wired up): a persistent, non-sticky promo bar above the header (`src/app/layout.tsx`); a two-tier sticky header — account/cart row plus a second dark category-links row (`src/components/site-header.tsx`), category data centralized in `src/lib/product-category.ts` (`CATEGORY_LABELS`) and `src/lib/category-icons.tsx` (`CATEGORY_ICONS`, lucide, kept out of the tRPC-router-shared file on purpose); a "Shop by category" icon-tile grid and a 3-item trust strip (Lebanon-based / online+in-store / secure checkout) on the homepage (`src/app/page.tsx`); and working category filter pills on `/products` driven by `?category=` (`src/app/products/page.tsx`, filters the already-fetched list client-side in the server component — no new tRPC endpoint needed given the small catalog). All copy in the trust strip/promo bar is deliberately conservative (no invented claims like "free shipping" or "next-day delivery" — those aren't confirmed).
- **Admin UI, full redesign (2026-07-13)**, done because the user is preparing to sell/hand off this site to a client and wanted the admin panel to look like a real product. Reference this time was a live inventory/POS admin (`alexph.one`) the user pointed at directly, screenshotted for structural/style inspiration only — not their brand, copy, or product data. Changes:
  - **Full dark theme for `/admin/*` only**: the admin layout wraps its content in a `dark` class (`src/app/admin/layout.tsx`), which flips all existing shadcn CSS-variable tokens (`bg-card`, `text-foreground`, `bg-primary`, etc.) to the dark palette already defined in `globals.css` — no new colors were hand-rolled. The sidebar specifically uses the `--sidebar`/`--sidebar-foreground`/`--sidebar-accent` tokens (not `--primary`) so it stays visually dark regardless of light/dark mode semantics elsewhere.
  - Two persistent CTA buttons at the top of the sidebar — "Scan to add" (gold, links to `/admin/products/new`) and "Scan to sell" (outline, links to `/admin/pos`) — mirroring the reference site's prominent top-bar actions.
  - **Products table rewrite** (`src/components/admin/products-table.tsx`, client component fed by a server-fetched list): search-by-name, category filter, sort (Name/Price/Stock/Sold/Revenue), real barcode graphics (new dependency: `jsbarcode`, wrapped in `src/components/barcode.tsx`), color-coded stock badges (green/amber/red), and row actions — **Sale** (deep-links to POS via `?variantId=`, auto-adds the item if the product has exactly one variant), **Edit**, **Archive/Restore**.
  - **New backend**: `product.adminList` (all products incl. archived, with `sold`/`revenue` aggregated from `OrderItem`s belonging to `PAID`/`SHIPPED`/`DELIVERED` orders only — pending/cancelled orders don't count), `product.byId`, `product.update` (diffs submitted variant rows against existing ones — updates by id, creates new, deletes removed; deleting a variant that already has order history throws a friendly `TRPCError` instead of a raw Prisma FK failure, since `OrderItem.variant` has no cascade/set-null), `product.setActive` (soft delete/restore via the existing `isActive` flag — **not** a hard delete, since hard-deleting a `Product` cascades to its variants and would hit the same FK problem for anything ever sold), `product.variantById`.
  - **New page**: `/admin/products/[id]/edit` — shares one form component (`src/components/admin/product-form.tsx`) with `/admin/products/new` instead of duplicating ~330 lines; the shared component switches between `create`/`update` mutations based on whether `initial` data was passed.
  - **Product form simplified** (user's explicit follow-up ask): removed the Description field entirely. When Category's age group ("Mens or kids?") is set to Kids or Mens, the variant list stops being a manual add/remove list and instead auto-populates one fixed row per size in that ladder (`sizeGridRows()` helper) — the admin just fills in a stock count per size instead of adding rows one at a time and picking a size each time. Non-sized categories (balls, trophies, gloves, shin pads, socks) keep the original flexible free-form variant list (size as text, Add/Remove buttons).
  - **Not yet done**: a manual (non-barcode) "pick product → pick size" add-to-sale flow in POS was floated as a possible reading of "in add and sell" but not built — only the barcode-scan path and the new `?variantId=` deep-link from the products table exist. Confirm with the user whether that's still wanted before building it, since it's a distinct chunk of work from what shipped.
  - Verified with real authenticated screenshots (headless Edge driven via raw Chrome DevTools Protocol — WebSocket + `Network.setCookie` to inject a real Auth.js session cookie obtained by replicating the login Server Action's progressive-enhancement form POST via `curl`; see the memory file's design notes for the exact technique) on the dashboard, products table, POS, new-product form, and edit-product form (confirmed real data — e.g. Goalkeeper Gloves' actual M/L variants and barcodes — loads correctly into the edit form). Build/lint/typecheck all pass clean.
- Admin UI redesign (2026-07-13): applied the same idea to `/admin`, `/admin/products`, `/admin/pos`, `/admin/products/new`. Reference this time was the shadcn dashboard-01 convention (sidebar 240–280px + KPI stat-card strip + data table) plus known POS UX conventions (tap-friendly chip-style controls, prominent totals) — couldn't screenshot `ui.shadcn.com`/`linear.app` live due to a TLS/DNS network issue in this environment that day (unrelated to those sites; `curl` failed with `CRYPT_E_REVOCATION_OFFLINE` and DNS errors on multiple hosts), so this pass leaned on well-established written patterns rather than fresh screenshots. Changes: `src/app/admin/layout.tsx` now has a fixed dark charcoal sidebar (`bg-primary`, matching the storefront header) with icon nav links via a new client component `src/components/admin/sidebar-nav.tsx` (`usePathname()`-based active-state highlighting — kept out of the layout itself so the layout can stay an async Server Component for the `requireAdmin()` check). `/admin` dashboard got a 4-up KPI strip (`src/components/stat-card.tsx`, reusable) computed from already-fetched product/variant data (products, variants, units in stock, out-of-stock count — no new backend query). `/admin/products` table now shows a category icon + badge per row and a stock-status badge (green via the `--brand` token if in stock, destructive if not). `/admin/pos` replaced the payment-method `<Select>` with tap-friendly pill buttons and made the total/complete-sale bar visually prominent (large accent button). `/admin/products/new` wrapped in a `Card` and its submit button switched to the `accent` variant for consistency with the other primary CTAs (New product, Complete sale, Scan item).
- **Pricing model (2026-07-13/14)**: `Product` gained `costPrice` and `salePrice` (`prisma/schema.prisma`, migration `20260713171723_add_cost_and_sale_price`). One shared helper, `src/lib/pricing.ts` (`effectiveUnitPrice`), is now the single source of truth for what a customer actually pays — precedence is variant `priceOverride` → `salePrice` → `basePrice` — and every previously-duplicated inline computation (cart, checkout, POS, `order.createPosSale`, `order.checkout`) now calls it instead of recomputing the same logic. `costPrice` is **never** returned by any normal product query — enforced server-side via Prisma's `omit: { costPrice: true }` at the query level (not a client-side hide), and is only ever returned by two PIN-checked procedures, `product.revealCostPrice` (single product, used by the product form) and `product.revealAllCostPrices` (bulk, used by the products table). The PIN is `COST_PRICE_PIN` in `.env` — a screen-privacy gate for a demo/handoff context, not a real auth boundary (the admin panel is already login-gated). Storefront (`ProductCard`, `/products/[slug]`) and the admin products table now show a strikethrough `basePrice` + highlighted `salePrice` whenever a sale price is set. Verified end-to-end with a scripted headless-Edge run that actually clicks "Show costs," types the PIN, and confirms the dialog closes and the Cost column appears (all "—" currently, since no seed product has a cost price yet — that's correct, not a bug).
- **Admin visual direction, third pass (2026-07-14)**: user pushed back hard ("WHAT IS THIS UI... SHOW ME OPTIONS") on redesigning blind again — this time, real screenshots of three concrete reference UIs were pulled first (shadcn's own `dashboard-01` example, TailAdmin's colorful chart-heavy template, and the existing alexph.one-inspired look) and the user picked via `AskUserQuestion` before any code changed. **Chosen: shadcn `dashboard-01` — near-monochrome, minimal, quiet borders.** Applied by removing gold `accent` usage from every admin surface (sidebar CTAs, "New product," "Scan item," "Complete sale," the product-form submit button all now use the plain neutral `default` button variant — color is reserved for real status semantics only: destructive/amber/brand-green stock states, not decoration). Sidebar restructured with a brand tile (`LS` + "Leader Sport", mirroring the reference's org-switcher row) and a muted "MENU" section label above the nav. `StatCard` (`src/components/stat-card.tsx`) dropped its icon entirely and now mirrors the reference's label/number/description rhythm — no fabricated trend percentages were added (no real historical baseline to compute them from), consistent with the project's existing "don't invent claims" convention; the one badge that does appear (Out of stock card) reflects a real computed state. Products table stock/archived badges rebuilt as quiet outline-badge-plus-colored-dot (`DotBadge` helper) instead of solid fills, closer to the reference's status-dot convention.
- **Bug found and fixed during the above (2026-07-14)**: the root layout (`src/app/layout.tsx`) rendered the storefront promo bar + `SiteHeader` unconditionally for *every* route, including `/admin/*` — so the customer-facing gold nav bar was stacking on top of the admin panel's own dark sidebar in every authenticated screenshot. Fixed by moving all storefront routes (`page.tsx`, `cart/`, `checkout/`, `login/`, `orders/`, `products/`, `register/`) into a new `(storefront)` route group with its own `layout.tsx` carrying the promo bar/header — route groups don't change URLs, so no links or redirects needed updating. Root `layout.tsx` now only owns fonts/`TRPCProvider`/`Toaster`/the outer `<main>`. Confirmed via fresh authenticated screenshots that `/admin/*` no longer shows any storefront chrome, and via plain `curl` that `/`, `/products`, `/cart` etc. still 200 at their original paths. `npm run build` confirms the route table is unchanged.
- **Reusable technique note**: driving the admin UI via headless Edge/CDP now goes beyond static screenshots — `Runtime.evaluate` can find-and-click a button by its text content and set a React-controlled `<input>`'s value via the native `HTMLInputElement.prototype.value` setter + a dispatched `input` event (React's synthetic event system requires this, not a plain `.value =` assignment) to actually drive dialogs/forms headlessly, not just observe pages. Used this to prove the PIN-unlock flow works end-to-end, not just that it typechecks.
- **Storefront redesign, third pass (2026-07-14, same day)**: after the admin work, user escalated: "do the ui for the whole website... nothing changed in the customer page admin page everything" — read as a directive to bring the *entire* site up to the same bar, not just admin. Tried to pull two more live references (Nike, Adidas) for inspiration but both dead-ended (Nike showed a region-picker interstitial, Adidas bot-blocked with a 403) — rather than burn more turns chasing screenshots, proceeded directly on the existing brand (charcoal/gold/green, established 2026-07-10) since the ask was about execution quality, not a brand change nobody requested. Changes: **added a real footer** (`src/components/site-footer.tsx` — brand blurb, category links, store info; conservative copy, no invented hours/socials) wired into the new `(storefront)/layout.tsx`; **`ProductCard`** (`src/components/product-card.tsx`) rebuilt with an image-panel layout (large tinted square with the category icon standing in for product photography, since Cloudinary still isn't wired up) instead of a small icon-in-header-row layout; **`AddToCart`** (`src/components/add-to-cart.tsx`) rebuilt from a `<Select>` dropdown to clickable size/color pill buttons (out-of-stock variants shown struck-through, not hidden); **product detail page** (`products/[slug]/page.tsx`) restructured into a two-column image+details layout, removing a redundant plain-text variant list that duplicated what `AddToCart`'s pills already show; **cart page** items now sit in one bordered `Card` with dividers instead of loose flex rows, with a proper empty state (icon + "Browse products" CTA); **checkout page** wrapped in a sectioned `Card`, payment method switched from a `<Select>` to pill buttons (consistent with the POS pattern); **login/register pages** wrapped in a centered `Card` with a heading/subheading; **order confirmation page** got a success-checkmark header and a sectioned `Card` for status/items/total. Homepage hero got a second outline CTA ("Browse categories," anchor-links to the category grid) and larger type. Verified with fresh headless screenshots of home, `/products`, a product detail page, and `/login` — all render correctly with the footer, pill pickers, and image-panel cards in place. `npm run build`/`lint`/`tsc --noEmit` all clean.
- **Branding + sizes polish (2026-07-14, same day)**: user liked the storefront pass but flagged the wordmark as "white light grey" (too flat/no real color) and asked for sizes to show up, and separately said the admin panel is on hold — they're planning a **separate app** for admin/POS later rather than continuing the web version. Fixes: new `src/components/logo.tsx` (a small gold `bg-accent` "LS" tile + the existing two-tone wordmark, reused in both header and footer) gives the brand an actual mark instead of plain text; `--accent` in `globals.css` richened from a pale `oklch(0.78 0.15 75)` to a more saturated amber `oklch(0.72 0.18 68)` (dark-mode `--accent` untouched — admin no longer uses it at all after the monochrome pass). `ProductCard` now shows in-stock size chips (deduped from `variants`, e.g. "S / M / L" or "Long / Short") between the name and price — `product.list()` already included `variants`, so this was purely a display change, no new backend query. **Admin is intentionally paused** — do not proactively redesign or extend `/admin/*` further; the user's stated plan is a separate dedicated app for admin/POS, decide with them before doing more web-admin work.
- **Color pass (2026-07-14, same day)**: user said the storefront still looked flat/white ("nothing changed change the background... make it colorful and stick with colors i gave u" — an explicit instruction to use *only* the existing charcoal/gold/green palette more, not introduce new hues). Fixes, all via existing `--accent`/`--brand` tokens (no new colors added): `--background` warmed from pure white to a barely-there warm off-white (`oklch(0.99 0.004 75)`); `ProductCard`'s image panel changed from a flat grey `bg-primary/5` square to a warm `bg-gradient-to-br from-accent/20 via-accent/5 to-brand/10` wash with a gold (`text-accent`) icon, and its category badge from grey `secondary` to a gold-tinted `bg-accent/15`; the same treatment applied to the product detail page's image panel for consistency; homepage trust-strip icons and "Shop by category" tiles recolored from grey `bg-primary/10` to the same warm gold tint (resting state), keeping their existing hover-to-solid-gold transition; the "Latest products" section got a very subtle `brand`-tinted wash at the top; `/products` gained a colored band (same gold/brand gradient) behind the page header and filter pills instead of one flat white slab, and filter pills switched from bordered-outline to solid `bg-card`/`bg-primary` chips for more visual weight. Verified with fresh screenshots of home, `/products`, and a product detail page — every page now has actual color in the body, not just the charcoal header/footer chrome.
- **Admin PWA (2026-07-14, same day)**: "admin as an app" turned out to mean an installable web app, not a native rebuild — confirmed via `AskUserQuestion` before building (options were PWA / React Native / Electron-Tauri desktop / "let's talk it through"; PWA was picked as the recommended, lowest-overhead option since it reuses the whole existing admin UI, including the camera barcode scanning that already works). Scoped entirely to `/admin` — the storefront has no manifest and never prompts customers to install anything:
  - `src/app/admin/manifest.webmanifest/route.ts` — a plain Route Handler, **not** the `manifest.ts` file convention, because that convention is root-`app/`-only (confirmed against this Next 16 version's own bundled docs, `node_modules/next/dist/docs/`, per `AGENTS.md`'s instruction to check local docs on this project rather than assume older Next.js behavior). Linked in via `metadata.manifest` in `src/app/admin/layout.tsx`'s new `metadata` export (`Metadata.manifest` accepts any URL, not just the special file), alongside an `appleWebApp` block for iOS home-screen install meta tags.
  - Icons: a shared `renderAppIcon()` helper (`src/lib/pwa-icon.tsx`) using `next/og`'s `ImageResponse` draws the gold "LS" mark at any size — `src/app/admin/icon.tsx` (browser-tab favicon) and `apple-icon.tsx` (iOS home-screen icon) use Next's file-convention icons scoped to `/admin` (confirmed via the same docs check that `icon`/`apple-icon`, unlike `manifest`, **are** valid at any nested segment); three more fixed-URL routes (`src/app/admin/icons/192|512|512-maskable/route.tsx`) exist purely so the manifest's `icons` array has stable URLs to point at (the file-convention icon URLs are hashed/not meant for that). Brand hex values (`#ec8a00` gold, `#0b0e11` admin dark bg) were derived by converting the actual OKLCH CSS tokens through the real OKLab→sRGB formula in a throwaway script, not eyeballed, so the icon matches on-screen color exactly.
  - Service worker: `public/admin/sw.js`, registered with explicit `scope: "/admin/"` by a small client component (`src/components/admin/pwa-register.tsx`, mounted in the sidebar). Deliberately does **not** cache pages or API responses — stock/order data changes constantly, so caching it would serve actively wrong numbers, not just stale ones. All it does is satisfy the installability requirement and swap in a friendly `/admin/offline` page instead of the browser's default error when there's no connection. The same component also listens for `beforeinstallprompt` and shows a small "Install app" button in the sidebar when the browser is willing to offer it (button only renders when that event has actually fired — not tested in headless Chrome since the install-prompt heuristics need real engagement signals, but the SW registration and manifest link were verified for real).
  - **Real bug caught and fixed**: `src/proxy.ts`'s matcher (`/admin/:path*`) was redirecting the manifest, icons, and service worker to `/login` along with every real admin page — which breaks PWA installation entirely, since a browser/OS needs to fetch those without a session. Fixed by carving out an explicit public allowlist (`PUBLIC_ADMIN_PATHS` + an `/admin/icons/` prefix check) inside the proxy function for exactly those paths — nothing else under `/admin` changed, confirmed via `curl` that `/admin/manifest.webmanifest` now 200s while `/admin/products` still 307s to `/login` when unauthenticated.
  - Verified end-to-end while actually logged in (not just build/lint): headless Edge navigated to `/admin` with an injected session cookie, and a script confirmed zero console errors/exceptions, the `<link rel="manifest">`/`<link rel="icon">`/`<link rel="apple-touch-icon">` tags are present with the right scoped URLs, and `navigator.serviceWorker.getRegistrations()` reports an **active** registration with `scope: "http://localhost:3000/admin/"`.
  - Admin is otherwise still paused for further *proactive* visual/feature work — this PWA layer is infrastructure, not a UI redesign; don't initiate more admin UI changes unprompted, but obviously still act on anything the user explicitly asks for (see next bullet — they did, immediately).
- **Admin products table thumbnail (2026-07-14, same day)**: user pointed at a screenshot of the products table and asked for the small grey category icon next to each product name to look like "the photo of the product as shown in the customer page" instead. Since there's no real product photography yet (Cloudinary still isn't wired up), the storefront's "photo" is itself the gold-gradient icon panel from `ProductCard`/the product detail page — so this was a one-line consistency fix: the admin table's product-row icon tile now uses the exact same treatment (`bg-gradient-to-br from-accent/20 via-accent/5 to-brand/10`, `text-accent` icon, `strokeWidth={1.25}`), just sized up to `size-11` to read as a thumbnail rather than a small badge (`src/components/admin/products-table.tsx`). Verified with a fresh authenticated screenshot.
- **Site-wide light/dark mode + cart preview dropdown (2026-07-14, same day)**: clarified via `AskUserQuestion` first — toggle on *both* storefront and admin (not just one), and "add cart" meant a preview dropdown from the header cart icon (cart itself already existed at `/cart`), not a missing feature.
  - **Theme**: `src/components/theme-provider.tsx` holds a tri-state preference (`"light" | "dark" | null`, `null` = "no explicit choice yet") in `localStorage`, applied as a `.dark` class on `<html>` — a client component was necessary here (not just per-layout classes) because a single real toggle needs one source of truth reactive to both user clicks and route changes. Each section keeps its **original designed default** when `theme` is `null`: storefront defaults light, admin defaults dark (`pathname.startsWith("/admin")`) — exactly as each was actually designed — but the moment a user toggles anywhere, that becomes one explicit site-wide preference from then on (same person, same browser). A `beforeInteractive` `next/script` in the root layout (`src/app/layout.tsx`) applies the same light/dark-by-section logic synchronously before paint, so there's no flash of the wrong theme; `suppressHydrationWarning` on `<html>` silences the resulting harmless className mismatch (the standard, well-known pattern for this — same one `next-themes` uses internally). Admin's layout no longer hardcodes a `dark` class on its wrapper div — it was redundant/actively wrong once the toggle exists, since it would have forced admin dark regardless of user choice; removing it is safe because CSS custom properties still cascade down to it from whatever class is on `<html>`. `ThemeToggle` (`src/components/theme-toggle.tsx`) lives in both `SiteHeader` (icon-only) and the admin sidebar (icon + "Light/Dark mode" label via a `showLabel` prop).
  - **A real design conflict, found and fixed, not just noted**: naively wiring the toggle would have broken the storefront's header/footer/hero, because those use `bg-primary text-primary-foreground` for the charcoal brand bar, and `--primary` is deliberately near-white in dark mode (so admin's default buttons stay visible on a near-black page — that's *why* it was tuned that way during the admin redesign). Toggling the storefront to dark would have flipped the header to a jarring bright-white bar. Fixed by adding a **separate** token, `--surface-brand`/`--surface-brand-foreground` (`globals.css`), defined only in `:root` and deliberately *not* redeclared in `.dark` — CSS custom properties not overridden by a more-specific-context rule simply keep cascading from `:root`, so this stays the same charcoal-on-white pairing in both modes, exactly as the brand bar should. `SiteHeader`, `SiteFooter`, and the homepage hero now use `bg-surface-brand`/`text-surface-brand-foreground` instead of `primary`; small button-like elements (active filter pills, payment-method pills) correctly kept using `--primary` since those *should* flip like normal buttons. Verified empirically with real screenshots of the storefront in both modes — not just reasoned about — confirming the header/footer/hero stay put while everything else (background, cards, trust strip) correctly inverts.
  - **Cart preview dropdown**: `src/components/cart-menu.tsx`, a client component using the existing `DropdownMenu` (Base UI) primitives. The header's cart icon+badge is now the dropdown trigger; full cart contents are fetched lazily via `trpc.cart.get.useQuery(undefined, { enabled: open })` only once the menu is actually opened (mirrors the existing lazy-reveal pattern from the cost-price PIN feature), so the header's initial server-rendered badge count stays cheap. Shows line items with size/color/qty, a running total (via the shared `effectiveUnitPrice` helper — same pricing source of truth as everywhere else), and "View cart"/"Checkout" buttons that close the menu on click. Verified end-to-end while logged in: opens, shows a real "Loading…" state, and settles to the correct "Your cart is empty" state with zero console errors.
- **Arabic/English + LBP display (2026-07-14, same day)**: scoped via `AskUserQuestion` first — Arabic real text but **LTR layout kept as-is** (no RTL mirroring, a deliberate simplification vs. standard Arabic UX), and LBP is **display-only** next to USD everywhere a price shows (not a real dual-currency payment flow — Whish Pay still only settles in USD). Covers the **entire customer-facing storefront** (header, footer, promo bar, homepage, `/products`, product detail, cart, checkout, login/register, order confirmation) — admin stays English-only, consistent with the standing "admin is paused unless asked" rule.
  - **Architecture**: no URL-based locale routing (no `/ar/...` segments) — a `locale` cookie (`src/lib/i18n/locale-cookie.ts` holds just the cookie name, kept in its own file so it has zero server-only imports and can be safely imported from client code) is the single source of truth, read server-side via `getLocale()` (`src/lib/i18n/get-locale.ts`, uses `cookies()`) and passed down as a plain `locale` prop through the component tree — deliberately **not** a React Context, since almost every storefront page is a Server Component and context only works client-side. Translation strings live in one file, `src/lib/i18n/dictionaries.ts` — `en` is the source of truth object, `ar: typeof en` gets a TypeScript structural check for free (a missing/extra key is a compile error, not a silent runtime fallback), plus a tiny `t(template, vars)` helper for `{count}`-style interpolation (deliberately simple — this app's plural needs are a stock count and a product count, not full Arabic 6-form plural grammar). `LanguageToggle` (`src/components/language-toggle.tsx`) writes the cookie and does a **full `window.location.reload()`**, not `router.refresh()` — chosen deliberately because some pages (checkout, login, register) are pure Client Components with no Server Component in their own tree, and `router.refresh()` only re-renders Server Components.
  - **A real hydration bug, found and fixed, not just a test artifact**: the first pass had `checkout`/`login`/`register` read the locale cookie via `document.cookie` directly inside the client component itself (a `getClientLocale()` helper used as a `useState` lazy initializer). This *seemed* safe by analogy to how `ThemeProvider` avoids a flash — but theme only ever mutates a DOM class in a `useEffect` (a side channel outside React's own diffing), while locale directly controls *rendered text*. Server-side, `document` doesn't exist, so the SSR pass always rendered English; client-side hydration immediately recomputed real Arabic text for the same DOM nodes — a genuine content mismatch, not a false alarm. It surfaced in headless verification as a confusing-looking React error ("Encountered a script tag while rendering React component" / `throwOnHydrationMismatch`) that took isolating single-page loads vs. multi-page sequences to actually pin down, since any *individual* page load in the default English locale was clean — it only reproduced with the `locale=ar` cookie already set. **Fix**: extracted each of those three into a `"use client"` presentational component (`src/components/login-form.tsx`, `register-form.tsx`, `checkout-form.tsx`) that takes `locale` as a required prop, with a thin new `page.tsx` in each route that's an `async` Server Component calling `getLocale()` and passing it down — the same pattern every other storefront page already used correctly. `get-client-locale.ts` (the flawed helper) was deleted outright once nothing referenced it anymore, rather than left around unused. Re-verified the exact same failing scenario (fresh `/login`/`/checkout` load with `locale=ar` cookie preset) headlessly after the fix: zero console errors.
  - **Currency**: `src/lib/currency.ts` — `LBP_PER_USD = 89_500`, a fixed placeholder rate (Lebanon has no single stable official rate) clearly commented as needing a real/live source before production; `formatLBP(usd)` just formats it with thousands separators. Never touches what's actually charged — `effectiveUnitPrice`/checkout/orders are still 100% USD; this is purely an extra `<p>` next to existing `$X` text.
  - Verified with real headless screenshots in both languages across home, `/products`, product detail, login, and checkout (with an authenticated but empty cart) — Arabic renders correctly with the layout staying left-to-right as scoped, LBP shows next to every price.
  - **Language toggle redesign**: user clarified they wanted a segmented pill (both "EN" and "AR" always visible in one rounded rectangle) with an animated sliding highlight, not a single button that swaps its own label. Rebuilt `LanguageToggle` accordingly — a `position: absolute` highlight div slides via a `transform: translateX()` computed from the active option's index × a fixed pixel width (kept in sync with the buttons' own `w-9`, not measured via a ref, since there are always exactly two fixed-width options), animated with a `transition-transform duration-300`. Since switching locale still requires a full `window.location.reload()` (see below), the click handler sets local `pending` state first (so the slide animation actually plays) and only reloads ~260ms later — long enough for the animation to be visible before the page tears down. Hit one real ESLint snag: the React Compiler's `react-hooks/immutability` rule flagged `document.cookie = ...` when that assignment lived directly inside the click handler passed into `OPTIONS.map(...)` — moved the cookie write into a small named function at module scope (`setLocaleCookie`) and the false positive went away; a pattern worth remembering if this rule flags similar global-mutation-inside-a-mapped-closure cases elsewhere. Verified with real screenshots at two points ~80ms apart around the click — confirmed the highlight is visibly mid-slide from the EN slot to the AR slot before settling, with zero console errors.
  - **Bug found and fixed (2026-07-14, later same day)**: user reported that switching to Arabic translated all the UI chrome (category badges, nav, etc.) but **not the product names themselves** — because `dictionaries.ts` only ever covered static UI strings, and `Product` had no Arabic-name field at all, so every product card/detail/cart/order line was always reading the single English `name` column regardless of locale. Fixed properly, not papered over: added `Product.nameAr` (optional `String?`, migration `20260714202317_add_product_name_ar`) with a single fallback helper `src/lib/i18n/product-name.ts` (`productName(product, locale)` → `nameAr` if set and locale is `ar`, else `name`) used everywhere a product name is rendered on the storefront — `ProductCard`, product detail page, cart page/`CartItemRow`, `CartMenu`, and the order confirmation page. `product.create`/`product.update` (`src/server/routers/product.ts`) accept an optional `nameAr` field; the admin product form (`src/components/admin/product-form.tsx`) gained one new "Name (Arabic)" text input (`dir="auto"`, optional) right under the existing Name field — this is a minimal functional addition, not a reopening of the paused admin redesign. Backfilled the 5 existing seeded products with real Arabic names via a one-off script (run once, then deleted — not part of the codebase); `prisma/seed.ts` also updated so future reseeds include `nameAr` from the start. Product `description` was deliberately **not** given an Arabic counterpart — the admin form has no description field at all anymore (removed in an earlier pass, see above), so there'd be no way to ever populate it.
  - **Adjacent bug also fixed while in this code**: `cart.get` and `order.byId` (`src/server/routers/cart.ts`, `src/server/routers/order.ts`) included the full `product` relation with no `omit: { costPrice: true }` — meaning `costPrice` was actually being sent to authenticated customers over the wire (visible in the network tab), contradicting the project's own stated invariant that cost price is never returned by any normal product query. Added the same `omit` used everywhere else in `product.ts`. Not something the user reported — found by inspection while touching this code, worth a security-review pass over the rest of the routers at some point to check for the same pattern elsewhere.
  - Verified for real: restarted the dev server (Prisma client needed regenerating — `npx prisma generate` — after the schema change, and the already-running dev process had the stale client cached in memory, so product queries kept returning `undefined` for `nameAr` until restart; a subtlety worth remembering next time a schema field is added while a dev server is already running), then `curl`'d `/products` and a product detail page both with and without the `locale=ar` cookie — Arabic cookie shows real Arabic names for all 5 products, no cookie/English shows the original English names unchanged. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean.
- **Circular theme toggle + guest cart visibility (2026-07-16)**: two small independent asks, both clarified via `AskUserQuestion` first since the phrasing was ambiguous.
  - **Theme toggle redesigned as a circle**: `src/components/theme-toggle.tsx`'s icon-only variant (used in `SiteHeader`) is now `size="icon"` + `rounded-full` + a subtle border, so it reads as a circle at rest, not just on hover — Sun/Moon are stacked in the same spot and cross-fade + rotate between each other on toggle instead of one icon being swapped for the other. The admin sidebar's labeled variant (`showLabel`) keeps its full-width row layout but wraps the same crossfading icon in a small circular badge next to the "Light/Dark mode" text, for visual consistency with the header version.
  - **Cart icon now visible to guests too**: previously `CartMenu` was only rendered in `SiteHeader` when `session?.user` existed, so logged-out visitors saw no cart affordance in the header at all. `CartMenu` now always renders and takes a new `isLoggedIn` prop; when a guest opens the dropdown it skips the `cart.get` query entirely (`enabled: open && isLoggedIn`, since that procedure is `protectedProcedure` and would just 401) and shows a "Log in to view your cart" prompt with a button to `/login` instead of attempting to load cart contents. Added a matching `cartMenu.signInPrompt` string to both `en`/`ar` dictionaries. `AddToCart` already redirected guests to `/login` on an `UNAUTHORIZED` mutation error, so this makes the header consistent with behavior that already existed elsewhere.
  - Verified: `npx tsc --noEmit` and `npm run lint` both clean; `curl`'d `/products` with no session cookie and confirmed the cart icon (`aria-label="Cart"`) is now present in the markup (previously absent for guests), and confirmed the theme toggle's rendered class list includes `size-8 rounded-full border border-current/20`.
- **"Light Steel" monochrome palette (2026-07-16, same day)**: user supplied an exact 9-step gray palette (Bright Snow `#f8f9fa` → Shadow Grey `#212529` — this is Bootstrap's gray-100..900 ramp; `#343a40`/Gunmetal already matched the existing charcoal `--primary`/`--surface-brand`, confirming continuity). Scoped via `AskUserQuestion` first: applies to **both** storefront and admin (not just one), goes **fully monochrome** (the old gold `--accent` and green `--brand` tokens are gone entirely, not just backgrounds), and dark mode is a **derived counterpart** using the same nine steps in reverse rather than left alone.
  - **Conversion, not eyeballing**: each hex was run through the real sRGB→OKLab→OKLCH formula (throwaway script, same rigor as the earlier PWA-icon color derivation) to get exact `oklch()` triples for `src/app/globals.css`, then round-tripped back OKLCH→hex to confirm zero drift before committing to the values. Every token in `:root`/`.dark` (background, foreground, card, popover, primary, secondary, muted, accent, brand, border, input, ring, all `--sidebar-*`) now maps to one of the nine given steps (plus pure white for light-mode card/popover, matching the ramp's implicit endpoint) — reused across roles rather than inventing anything new. `--destructive` (red) is the one deliberate exception, kept because it's a functional safety signal (delete/error states), not brand decoration — flagged to the user as a judgment call, not silently deviated.
  - **Mode-flip pattern preserved**: kept the same structural pattern the old gold/green tokens used — `--primary` flips dark↔light between modes (Gunmetal in light, Platinum in dark) exactly as before; `--accent` stays a fixed light-ish step (Pale Slate darker, `#adb5bd`) with fixed dark `--accent-foreground` (Shadow Grey) in **both** modes, mirroring how gold `--accent` always paired with dark `--accent-foreground` regardless of mode; `--brand` flips (Iron Grey light / Pale Slate lighter dark), mirroring how green `--brand` used to flip. `--surface-brand` (header/footer/hero band) stays Gunmetal, unchanged, still `:root`-only per the existing "constant brand bar" design.
  - **Admin picked this up for free**: admin's layout no longer hardcodes its own dark styling (removed when the light/dark toggle was built, see the entry above) — it reads the same `:root`/`.dark` tokens as the storefront, so retheming `globals.css` once covers both sides with no admin-specific CSS changes needed. Verified with a real authenticated screenshot of `/admin/products` (logged in via the existing `owner@leadersport.local` test account, explicitly re-confirmed okay to keep using via `AskUserQuestion` after the permission layer flagged the pattern) — sidebar, table, badges, and buttons all render in the new steel palette; the one non-monochrome accent, destructive-red delete icons, shows up correctly as the deliberate exception.
  - **Real contrast bug caught and fixed, not just a style swap**: the product-card/product-detail/admin-table "photo stand-in" icons (`Icon` over a `from-accent/20 via-accent/5 to-brand/10` wash, meant to look like a product photo since there's no real photography yet) used `text-accent` directly. With gold, this read fine even at similar lightness to the background because hue alone gave contrast; once desaturated, an icon at the same lightness as its wash became nearly invisible — confirmed visually via a real headless screenshot (icons were near-invisible pale-on-pale in the first render). Tried `text-accent-foreground` first, but that's fixed-dark in both modes (by design, see above) — would fix light mode but silently break dark mode (dark icon on a dark-mode wash-over-dark-card), since **both the storefront and admin now support the same light/dark toggle**. Fixed correctly with `text-muted-foreground` instead (`src/components/product-card.tsx`, `src/app/(storefront)/products/[slug]/page.tsx`, `src/components/admin/products-table.tsx`), which already flips per mode (Slate Grey in light, Pale Slate darker in dark) — verified with fresh screenshots in both modes that icons are now clearly visible with good contrast in each.
  - **Hardcoded colors outside the CSS variables, found by a dedicated pre-flight search and fixed in lockstep**: `src/lib/pwa-icon.tsx` (admin PWA icon generator, `next/og`'s `ImageResponse` can't read CSS custom properties) had gold hex baked in (`#ec8a00` tile, `#311800` text) — updated to `#adb5bd`/`#212529` (the exact new accent/accent-foreground steps). `src/app/admin/manifest.webmanifest/route.ts`'s `background_color`/`theme_color` (`#0b0e11`, derived from the old dark background) updated to `#212529` (Shadow Grey, the new dark-mode background, used directly since it's one of the user's literal given hex values — no re-derivation needed). Nothing else in the codebase had a hardcoded color bypassing the token system (confirmed by a dedicated search pass before touching anything, covering arbitrary-value Tailwind classes, inline `style=`, and SVGs).
  - Verified end-to-end: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean; real headless screenshots of `/products` in light mode (post-icon-fix) and dark mode (toggled via a real click through CDP, not just a class override), plus an authenticated `/admin/products` screenshot — all confirm the steel palette renders correctly, product icons are visible, prices/badges/status dots are all neutral gray instead of gold/green, and the header/footer brand band stays Gunmetal in both modes as designed.
- **Real logo + branded splash screen + "quiet reload" system (2026-07-16, same day, undocumented until now)**: this work happened earlier the same day but was never written up here — reconstructed from the actual code state after a `/compact` attempt got interrupted (`AbortError: Compaction canceled`) and left this session without a bridging summary. Real logo assets now exist (`public/logo-mark.png`, `logo-mark-light.png`; `logo-splash.png` also exists but is currently unreferenced — an earlier draft, left in place rather than guessed-and-deleted). `src/components/logo.tsx` uses `logo-mark-light.png` in the header/footer. `src/app/loading.tsx` was rebuilt into a full-screen branded splash (`logo-mark.png` + "LEADER SPORT" wordmark + bouncing-dots loader on black) — the App Router `loading.js` Suspense fallback shown during real page transitions. `src/app/(storefront)/layout.tsx` deliberately holds that splash on screen for a flat 3 seconds on genuine loads (`await new Promise(resolve => setTimeout(resolve, 3000))`) so it doesn't just flash by — a deliberate, previously-requested UX choice, not a bug.
  - **"Quiet reload" system**: `src/lib/quiet-reload-cookie.ts` defines `QUIET_RELOAD_COOKIE` (`ls-quiet-reload`), a short-lived (`max-age=5`) marker cookie for "this reload was triggered intentionally by app code, not a real user hard-refresh." `LanguageToggle` sets it right before its `window.location.reload()` (needed for the new locale to take effect server-side); `(storefront)/layout.tsx` skips its 3-second hold when the cookie is present. `src/components/reload-home-redirect.tsx` (`ReloadHomeRedirect`, mounted in the root layout) is a separate, related feature: it sends the user back to `/` whenever they manually hard-refresh the browser from anywhere on the site (detected via `performance.getEntriesByType("navigation")[0].type === "reload"`), but is skipped for quiet reloads so the language toggle doesn't accidentally bounce the user to the homepage — it also consumes/clears the cookie once read.
  - **Gap found and closed (2026-07-16, later same day)**: the quiet-reload cookie was being set and read by `(storefront)/layout.tsx` and `ReloadHomeRedirect`, but `loading.tsx` itself never checked it — so switching language still skipped the 3-second hold correctly, but the splash still rendered (briefly) as the default Suspense fallback for that reload, which is what the user actually saw and reported ("with the change language it shows the logo at first"). Fixed by adding the same cookie check to `loading.tsx` (`if (cookieStore.get(QUIET_RELOAD_COOKIE)) return null;`). Verified with a real CDP-driven timing test (not guessed): a genuine first `/products` load takes ~3.17s with the splash fully visible throughout (confirms the intentional hold still works), while triggering the language toggle afterward now completes in ~65ms with the splash never appearing in the DOM at all.
  - **User confirmed via `AskUserQuestion`**: keep the splash for real page loads, only suppress it for the language-switch reload — matching what this system already did once the `loading.tsx` gap was closed, no further scope change needed.
- **Real hydration bug found and fixed (2026-07-16, same day)**: user hit a genuine React hydration-mismatch error (not a false positive) on `ThemeToggle` — server rendered `aria-label="Switch to dark mode"`, client rendered `aria-label="Switch to light mode"` for the same element. Root cause: `theme-provider.tsx`'s `useState<Theme>(readStoredTheme)` lazy initializer read `localStorage` **during the hydration render itself** — safe-looking (mirrors the "read once, mirrored by an effect" pattern used elsewhere) but actually unsafe, since a `useState` initializer runs synchronously on the client's first render, before any effect, so a user with a previously-stored "dark" preference hydrated with a different `theme` value than what the server (which has no `localStorage`) had just rendered. This is the same root-cause *class* as the earlier locale hydration bug (see the Arabic/English entry above) — reading browser-only state somewhere that affects the first render's actual output — just newly present here, not previously caught. Fixed properly: rewrote `ThemeProvider` to use `useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot)` instead of `useState`+`useEffect` — `getServerSnapshot` always returns `null` (matching SSR), and React's own hydration-mismatch-avoidance machinery resyncs to the real `getSnapshot()` value immediately after mount, before the eslint `react-hooks/set-state-in-effect` rule even becomes relevant (a plain `useEffect(() => setThemeState(readStoredTheme()), [])` was tried first and correctly flagged by that rule — cascading setState-in-effect is exactly the anti-pattern `useSyncExternalStore` exists to replace). Same-tab updates need a manually-dispatched `StorageEvent` in `setTheme()`, since the native `storage` event only fires in *other* tabs. Verified with a real CDP reproduction of the exact reported scenario (set `localStorage` theme to `"dark"`, then a genuine hard reload, not a soft navigation) — zero hydration-related console messages afterward, and `document.documentElement` correctly ends up with the `.dark` class.
- **Confirmed intentional, not a bug**: `LanguageToggle`'s sliding highlight uses a hardcoded `bg-orange-500`/`text-white` instead of the steel-gray `--accent` token — asked the user directly via `AskUserQuestion` given it's the one spot of color left after the fully-monochrome palette change, and they confirmed it's deliberate. Left as-is.

## Payments — Whish Pay (important caveat)
Whish Money doesn't have a public, stable developer portal (unlike Stripe). What was found while researching (2026-07-09):
- Their merchant product is **"Whish Pay"** — a hosted-checkout gateway. You call a "Collect" endpoint with a merchant secret + order details, get back a redirect URL, and the customer pays on that hosted page using **either their Whish wallet or a Visa/Mastercard card** — one integration covers both payment methods the user asked for.
- The one technical spec PDF that surfaced in search (`lebsol.com/.../Whish-Collect-Web-Service-Technical-Specification.pdf`) is now a dead link. A reseller's docs (`pay.codnloc.com/api_documentation.html`) are access-restricted without an account.
- **Nothing in `src/lib/payments/whish.ts` has been verified against a live endpoint.** It's a best-effort placeholder shape (`POST {baseUrl}/collect` with a secret + order fields, expects `{ url, id }` back) behind a clean `PaymentProvider` interface (`src/lib/payments/types.ts`), so wiring in the real contract later is a small, isolated change — not a rewrite.
- Before this works: sign up for a Whish Pay merchant account (their site points to contacting them directly, no self-serve API reference), get real docs + credentials, set `WHISH_API_BASE_URL` / `WHISH_API_SECRET` in `.env`, and confirm the field names. Also implement real signature verification in the webhook stub (`src/app/api/webhooks/whish/route.ts`) — it currently trusts the payload as-is, which is not safe for production.
- Pricing is in **USD** (`amount`/`currency` in the checkout flow) — common practice for Lebanese online stores given LBP volatility. Flag if you want LBP instead.
- Stripe was removed entirely (not available as a Lebanese merchant).

## Environment-specific corrections (discovered while scaffolding, 2026-07-09)
This project runs on **Next.js 16.2** and **Prisma 7**, both newer than typical training-data knowledge — the following diverge from older/common patterns and are easy to get wrong if "fixed" from memory:
- Next.js renamed `middleware.ts` → **`proxy.ts`** (exported function name `proxy`, or default export). `middleware.ts` is deprecated. Ours lives at `src/proxy.ts`.
- `params`/`searchParams`/`cookies()`/`headers()` are fully async now (must `await`) — no synchronous fallback left.
- Prisma 7's `prisma-client` generator (used here, output at `src/generated/prisma`) has **no `index.ts` barrel** — import from `@/generated/prisma/client`, not the bare `@/generated/prisma` directory.
- Prisma 7's `prisma-client` generator **requires an explicit driver adapter** — `new PrismaClient()` with no args throws a type error. We use `@prisma/adapter-pg` (`src/lib/prisma.ts`); swap to `@prisma/adapter-neon` later if you want Neon's HTTP-based edge driver instead of standard TCP.
- Prisma config (env var loading, migration path) now lives in `prisma.config.ts`, not `package.json`.
- zod is v4: use top-level `z.email()` instead of `z.string().email()`, and `{ error: "..." }` instead of `{ message: "..." }` for custom messages.
- Auth.js v5 type augmentation: augmenting `"next-auth/jwt"` for the `JWT` interface **does not merge** (it's a wildcard `export *` re-export that TS won't merge into) — augment `"@auth/core/jwt"` directly instead. `"next-auth"` augmentation for `Session`/`User` works fine (named re-export). See `src/types/next-auth.d.ts`.
- Env var names changed for Auth.js v5: `AUTH_SECRET` (not `NEXTAUTH_SECRET`).
- This shadcn/ui version generates components on top of **Base UI** (`@base-ui/react`), not Radix. Notably `Select`'s `onValueChange` can receive `null` (`string | null`, not just `string`) — every `onValueChange` handler in this codebase guards for that (`value && ...`).

## Open Questions
- None blocking right now. Whish merchant credentials are the main external dependency — see "Payments" above.

## Gotcha: restart the dev server after editing `.env`
Next.js only reads `.env` when the dev server process starts — it does **not** hot-reload env var changes into an already-running `npm run dev`. If you change `DATABASE_URL` (or any other env var) and still see the old behavior, the fix is to stop and restart the dev server, not to change more code. This caused real confusion on 2026-07-09: the same `ECONNREFUSED` error kept showing up because the running dev server (from before `DATABASE_URL` was set) was never restarted.

## Gotcha: don't self-reference custom properties in the `@theme inline` block
`src/app/globals.css` had `--font-sans: var(--font-sans)` — a circular reference instead of pointing at the Geist font variable Next injects (`--font-geist-sans`, set via `layout.tsx`'s `geistSans.variable` on `<html>`). A self-referencing custom property is invalid CSS with **no build error and no console warning** — Tailwind's `font-sans` utility silently resolved to nothing and the whole site fell back to the browser's default serif font. Fixed 2026-07-10 (now `--font-sans: var(--font-geist-sans)`, matching the working `--font-mono: var(--font-geist-mono)` line right below it). If a future theme edit reintroduces unstyled/wrong-font pages with a clean build, check this block first — Tailwind won't error on it.

## Current Status (as of 2026-07-14)
**Database connected, storefront redesigned twice, admin panel redesigned three times (now on a monochrome shadcn-`dashboard-01` direction the user explicitly picked from real screenshotted options), full cost/sale pricing model wired end-to-end, single-admin enforced — all verified logged in as a real admin, not just build/lint.** Real Neon Postgres is wired up, migrations applied (including `add_cost_and_sale_price`), and `prisma/seed.ts` seeded 5 sample products. Confirmed working: `npm run build`/`npm run lint`/`npx tsc --noEmit` all pass clean, a live `npm run dev` serves `/`, `/products`, `/products/[slug]`, `/admin` (307-redirects when unauthenticated), and a scripted headless-Edge session (see "Reusable technique note" above) actually clicked through the PIN-gated "Show costs" flow on `/admin/products` and confirmed it unlocks correctly. `DATABASE_URL` now uses explicit `sslmode=verify-full` (was `sslmode=require`, which pg's driver warns is a deprecated alias) — no functional change, just silences a Node console warning and future-proofs against a breaking change in `pg-connection-string` v3.

A production-safe error boundary (`src/app/error.tsx`) was also added so a DB outage (or any other render-time error) shows a friendly message instead of a raw crash.

What exists:
- Next.js 16 app (TypeScript, Tailwind v4, App Router, Turbopack) at the project root. **Route structure**: storefront routes (`/`, `/products`, `/cart`, `/checkout`, `/login`, `/register`, `/orders`) live under a `(storefront)` route group with their own `layout.tsx` (promo bar + `SiteHeader`); `/admin/*` has its own separate layout and does **not** get the storefront chrome (fixed 2026-07-14, see "Decisions Made" — route groups don't change URLs). Storefront theme: charcoal `#343a40` primary + gold accent + green `--brand` token for prices. Admin theme: near-monochrome dark (shadcn `dashboard-01`-inspired, see "Decisions Made") — gold `accent` is intentionally *not* used anywhere in `/admin/*` anymore. Entrance/hover animations throughout (hero, product cards, admin quick-action cards).
- Prisma schema (`prisma/schema.prisma`): `User`/`Account`/`Session`/`VerificationToken` (Auth.js, single-admin constraint), `Product`/`ProductVariant` (with `ageGroup`, `barcode`, `costPrice`, `salePrice`), `Cart`/`CartItem`, `Order`/`OrderItem` (with `channel`, `paymentMethod`, `paymentReference`).
- Auth.js credentials auth (`src/auth.ts`) with signup/login/logout Server Actions (`src/app/actions/auth.ts`) and pages (`/login`, `/register`).
- tRPC wired end-to-end: routers (`src/server/routers/`: `product`, `order`, `cart`), route handler (`src/app/api/trpc/[trpc]`), client provider (`src/trpc/react.tsx`), server-side caller for Server Components (`src/trpc/server.ts`).
- Storefront: a non-sticky promo bar + two-tier sticky header (account/cart row, category-links row — `src/components/site-header.tsx`) sit above every storefront page (`src/app/(storefront)/layout.tsx`). Home (`/`) has a hero, a 3-item trust strip, a "Shop by category" icon-tile grid, and a latest-products grid. `/products` has working category filter pills (`?category=`) plus the same product grid. `/products/[slug]` has add-to-cart and shows a strikethrough base price + sale price when the product has one. Also: `/cart`, `/checkout` (Whish/Visa selection → payment provider → redirect), `/orders/[id]` (confirmation/status page). Product cards are one shared component (`src/components/product-card.tsx`, also sale-price-aware); category labels/icons are centralized in `src/lib/product-category.ts` and `src/lib/category-icons.tsx`.
- Admin section (all behind `requireAdmin()` + `proxy.ts`, and now DB-enforced to a single account), full dark theme, redesigned three times (see "Decisions Made" — most recently a monochrome shadcn-`dashboard-01` direction the user picked from real screenshotted options):
  - `/admin` dashboard: dark sidebar (brand tile + "MENU" nav group) with two persistent neutral CTA buttons ("Scan to add" / "Scan to sell"), a 4-up KPI stat-card strip (Products/Variants/Units in stock/Out of stock — no icons, no fabricated trend %, just real numbers + an honest status badge on the Out-of-stock card), quick-action cards
  - `/admin/products` — searchable/filterable/sortable table with category icons, quiet outline+dot stock/archived badges, real barcode graphics, Sold/Revenue columns (from real order history), strikethrough base price + sale price when set, a PIN-gated "Show costs" toggle (`product.revealAllCostPrices`) that adds a Cost column, and Sale/Edit/Archive row actions
  - `/admin/products/new` and `/admin/products/[id]/edit` — share one form component (`src/components/admin/product-form.tsx`). No description field. Category + "Mens or kids?" selectors; picking Kids/Mens auto-populates one row per size in that ladder (just fill in stock), non-sized categories keep a flexible manual variant list. Barcode field has a camera-scan button (`src/components/barcode-scanner.tsx`). Base price / sale price side by side, plus a PIN-gated "Cost price (private)" section (`product.revealCostPrice`)
  - `/admin/pos` — **scan to sell**: camera-scan a barcode (`product.byBarcode`) or arrive via `?variantId=` from the products table's Sale action, builds a running sale with tap-friendly payment chips (using `effectiveUnitPrice` from `src/lib/pricing.ts` for correct sale-price-aware totals), "Complete sale" creates an `IN_STORE` order, decrements stock
- Pricing: `src/lib/pricing.ts`'s `effectiveUnitPrice()` is the one place "what does the customer pay" is computed (variant override → sale price → base price) — used by cart, checkout, and both POS/order-router code paths. `costPrice` is never sent to the client except through the two PIN-gated procedures (`COST_PRICE_PIN` in `.env`).
- Webhook stub for Whish payment confirmation: `src/app/api/webhooks/whish/route.ts` (not signature-verified yet).
- shadcn/ui components in use: button, card, input, label, badge, table, dropdown-menu, dialog, select, separator, sonner, avatar, textarea. (`accent` variant still exists on `Button`/`Badge` for storefront CTAs — just no longer used anywhere under `/admin`.)
- `.env` — real Neon `DATABASE_URL` (`sslmode=verify-full`), dev-only `AUTH_SECRET`, `COST_PRICE_PIN`, blank Whish/Cloudinary keys — plus `.env.example` for reference. `prisma/seed.ts` reseeds the 5 sample products (`npx tsx prisma/seed.ts`) — none of them have a cost or sale price set yet, so the Cost column and sale-price strikethrough will show empty/absent until an admin sets one.

## Next Steps
1. Sign up a user at `/register`, then manually flip their `role` to `ADMIN` in the database (e.g. via `npx prisma studio`) — remember only one user can ever hold that role.
2. Try the barcode scan flows on a phone/webcam (`/admin/products/new` and `/admin/pos`) to confirm camera access works end to end — untested on a real device so far.
3. Get a real Whish Pay merchant account and credentials, then finish `src/lib/payments/whish.ts` and the webhook signature verification (see "Payments" above) — right now checkout will fail with a clear "not configured" error rather than silently pretending to work.
4. Wire up Cloudinary for product image uploads on the admin product form (currently `images` is an empty array on create).
5. Order list/detail views still don't exist (product *edit* is now done, see "Decisions Made" — admin redesign entry).
6. Confirm the single-admin interpretation is what was actually wanted (see the "Decisions Made" note above) — it was inferred from a somewhat ambiguous request; if a separate fixed admin login (outside the User/Role system) was meant instead, that's a different, larger change.
7. Once real product photos exist (needs Cloudinary wiring, see #4), revisit the product cards/hero — several ideas from the design-inspiration pass (large hero photography, image-led category tiles) were skipped specifically because there were no real images to use yet.
8. A real `owner@leadersport.local` admin account now exists (password `LeaderSportAdmin2026!`, created 2026-07-13 for design verification, explicitly authorized) — change its password or delete it via Prisma Studio once a real personal admin account is set up (see step 1).
9. Set a real, non-default `COST_PRICE_PIN` before handing this off to a client — `4477` is a dev placeholder.
10. Decide whether a manual (non-barcode) "pick product → pick size" add-to-sale flow is wanted in POS — this was a possible reading of an earlier request that didn't get built (only the barcode-scan path and the `?variantId=` deep-link from the products table's Sale button exist). See the admin redesign entry in "Decisions Made" for context.
11. Test the admin PWA install flow on a real phone/tablet, not just headless verification — open `/admin` in Chrome (Android) or Safari (iOS), confirm the "Install app" prompt/button actually appears and installs to the home screen correctly, and that camera barcode scanning still works from the installed app shell. Also worth trying on the actual production domain once deployed, since install heuristics can behave differently than on `localhost`.
12. `LBP_PER_USD` in `src/lib/currency.ts` is a fixed placeholder (89,500) — replace with a live/frequently-updated exchange rate source before real launch, since Lebanon's LBP has no single stable official rate and this will drift out of date.
13. Arabic translation currently covers the whole customer storefront but **not** `/admin/*` — admin is still English-only (deliberately, per the standing "admin is paused" rule). If Arabic admin is wanted later, the same `src/lib/i18n/dictionaries.ts` system extends there directly — just add an `admin` section and thread `locale` through the admin layout the same way.
14. Product names now support Arabic (`Product.nameAr`, see "Decisions Made" 2026-07-14 entry) but variant `size`/`color` text (e.g. "Long"/"Short", "Red") is still always English/whatever the admin typed — the size ladders (S/M/L, kid sizes, glove sizes) are numbers/Latin letters so that's likely fine as-is, but free-text sizes/colors on non-sized categories aren't translated. Only worth doing if the user asks — no Arabic admin input exists for it yet either.

---
*Update this file after notable decisions or milestones. A mirrored summary lives in Claude's project memory — keep both in sync.*
